---
name: limit-up-gene-cycle
description: 筛选中国A股主板在行情或行业启动周期内具有高频、高成功率、快速封板“涨停基因”，且近3个月呈底部放量形态的股票，并以涨停池、炸板池和独立前复权日线交叉验证，输出可审计的Excel/CSV分析。用户提到涨停基因、快速涨停、行业板块异动、行情启动、底部放量、涨停池、炸板率、封板成功率、短线候选股或要求复现这套A股量化筛选流程时使用。
---

# 行情启动周期内具有涨停基因的股票

把“涨停基因”视为可验证的历史行为，不把单次涨停当作结论。先用封板、炸板、首次封板时间和行业共振衡量基因，再用近3个月日线验证底部位置、持续放量和趋势修复。

## 执行顺序

1. 确定样本和截止日。
2. 逐交易日抓取近6个月涨停池与炸板池。
3. 计算涨停频率、封板成功率、快板率和行业联动率。
4. 对候选股抓取近3个月前复权日线，计算底部放量形态。
5. 用独立日线逐日核对涨停事件。
6. 应用硬门槛、排序、风险标注并生成表格。
7. 完成数据、公式和视觉质量检查后交付。

详细公式、字段、数据源和异常处理见 [references/methodology.md](references/methodology.md)。只有在更改阈值、替换数据源、解释分数或排查异常时读取该文件。

## 1. 锁定口径

- 默认样本：沪深主板普通A股，代码前缀 `000/001/002/003/600/601/603/605`。
- 排除创业板、科创板、北交所、名称含 `ST` 或退市标记的股票。
- 默认使用最近一个完整交易日，不使用未收盘或尚未完成数据更新的当天数据。
- 近6个月用于识别涨停基因；近3个月用于识别底部放量。
- 普通主板封板的日线复核阈值默认使用收盘涨幅 `>=9.3%`，容纳最小价格单位导致的 `9.9x%`。
- “快速涨停”默认定义为首次封板不晚于 `10:00:00`；同时保留 `10:30:00` 宽口径供审阅。

若用户指定其他市场、ST、时间窗口或阈值，单独分组计算，不要与默认样本直接混排。

## 2. 运行确定性筛选

优先运行随附脚本：

```bash
python3 scripts/screen_limit_up_gene.py \
  --end YYYY-MM-DD \
  --output work/limit_up_gene_analysis.json
```

常用参数：

```bash
python3 scripts/screen_limit_up_gene.py --help
```

脚本仅依赖Python标准库，输出元数据、精筛结果、观察池、全量候选、封板/炸板事件和候选日线。把缓存放在任务工作目录；不要把临时数据写入用户主目录或技能目录。

数据源失败时先重试并使用缓存。仍失败则说明缺失范围与影响，不得用随机数、旧榜单或凭印象补齐。

## 3. 解释评分

必须同时展示原始指标和分数，至少包括：

- 近6个月封板次数、炸板次数和触板次数。
- 封板成功率。
- `10:00` 前快速涨停次数与快板率。
- 首次封板时间中位数、最大连板数、近30日涨停数。
- 行业热日次数和行业联动率。
- 近3个月区间位置、距低点幅度、5日/基准20日量比、近10日持续放量天数。
- MA20斜率、近20日涨跌幅、日线事件复核一致率。
- 涨停基因分、底部放量分、综合分和风险提示。

不要把“高历史频率”写成“未来一定涨停”。使用“历史行为较稳定”“当前形态满足量化条件”等表述。

## 4. 应用默认硬门槛

默认精筛需同时满足：

- 近6个月封板次数 `>=5`。
- 涨停基因分 `>=55`。
- 底部放量分 `>=50`。
- 封板成功率 `>=65%`。
- 形态状态为“确认”或“强确认”。
- 独立前复权日线复核一致率 `>=90%`。

未通过全部门槛但接近的股票放入观察池，并逐项写明未通过原因。不要悄悄放宽门槛来凑数量。

## 5. 生成交付表格

用户要求Excel时，使用可用的电子表格技能生成 `.xlsx`，并遵守其公式、来源、渲染与验证要求。至少创建：

1. `总览`：统计窗口、样本量、事件量、精筛数量和Top 10。
2. `精筛结果`：所有核心指标、结论、风险、公式评分和来源ID。
3. `观察池`：接近门槛的候选及未入选原因。
4. `全量候选`：可筛选的完整候选表，评分与入选状态用公式重算。
5. `涨停事件`：精筛股每次封板/炸板事件和首次封板时间。
6. `日线复核`：近3个月前复权OHLCV、5日均量、20日基准均量及量比。
7. `方法与口径`：所有阈值、公式和限制。
8. `来源与检查`：纯文本URL、数据量核对、公式错误扫描和最终状态。

证券代码使用 `SH600000` / `SZ000001` 形式，避免前导零丢失。日期、百分比和数值必须存为可排序的类型，不要全部写成文本。

## 6. 强制质量门

交付前确认：

- 使用有效交易日过滤周末和交易所休市日；不得把休市日接口回退数据计为新事件。
- 精筛股的封板事件与独立日线逐日核对，一致率达到门槛。
- 精筛数量由全量候选表的公式门槛重新计算并与报告数量一致。
- 公式错误扫描没有 `#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?` 或意外 `#N/A`。
- 所有工作表完成可视化渲染检查，标题、代码、长风险提示和URL未被截断。
- 明确数据截止日、数据缺口、过热量能、行业联动偏弱、近期无触发等风险。
- 最终答案附“不构成投资建议”，并将结论定位为历史统计与技术筛选。

## 7. 禁止事项

- 不使用未来数据或盘中未完成数据计算收盘指标。
- 不把涨停次数等同于可交易收益率。
- 不删除炸板反证，不只统计成功案例。
- 不把不同涨跌幅制度的板块混在同一概率分母中。
- 不以单日巨量替代持续放量，也不把高位放量误称为底部放量。
- 不凭空补充公告、基本面、龙虎榜或监管结论；需要时另行检索并标注来源。
